#%PAM-1.0
auth       include      system-auth
account    include      system-auth
password   include      system-auth
# Enforce per-user session lock via PAM hook
session    required     pam_exec.so seteuid /usr/libexec/kaleidos/ly-session-verify.sh
session    include      system-auth